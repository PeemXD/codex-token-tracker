{
  "name": "codex-token-tracker",
  "version": "1.0.0",
  "description": "Local OTLP receiver and dashboard for tracking Codex token usage.",
  "type": "module",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
